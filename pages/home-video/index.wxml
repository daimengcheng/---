<!--pages/home-video/index.wxml-->
<view class="container">
  <view class="video-item" wx:for="{{topMvs}}" wx:key="id">
    <video-list-item 
      itemInfo="{{item}}" 
      data-item="{{item}}" 
      bindtap="playVideoHandler">
    </video-list-item>
  </view>
</view>
